// import * as React from 'react';
// import { useMemo } from 'react';
// import { TaskType } from '../types';
// //import { convertUnit } from '../store/type-store';
// import { Typography } from '@mui/material';

// export default function Conversionx({task, from, to}: {task: TaskType, from: string, to: string}) {
//    const fare = useMemo(() => convertUnit(task, from, to), [task, from, to]);

//     return (
//         <Typography variant="h4" component="h1" sx={{ flexGrow: 1 }}>
//             Fare : {fare.toString()}
//         </Typography>
//     );
// }